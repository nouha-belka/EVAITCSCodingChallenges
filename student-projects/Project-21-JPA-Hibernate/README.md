# Project 21: JPA & Hibernate - "BuildMyStudentDatabase"

## 🎯 Objective
Build a **Student Enrollment System** with JPA/Hibernate for database persistence.
Learn entity mapping, relationships, Spring Data JPA repositories, and JPQL.

## 📚 Topics Covered
- JPA Entity Mapping (@Entity, @Id, @Column, @Table)
- Relationships (@OneToMany, @ManyToOne, @ManyToMany)
- Spring Data JPA (JpaRepository — zero-SQL CRUD!)
- Custom JPQL Queries (@Query)
- H2 in-memory database for development
- Transaction Management (@Transactional)

## 🚀 How to Run
```bash
mvn spring-boot:run
# API:        http://localhost:8080/api/students
# H2 Console: http://localhost:8080/h2-console
```

